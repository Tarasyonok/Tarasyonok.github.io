<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>pvz almanac</title>
    <link rel="stylesheet" href="css/index.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
</head>

<body>
    <h1 class="text-center mb-5 mt-3">Админка <a href="/" class="btn btn-success">На главную</a></h1>
    <div class="container">
        <form action="/admin">
            <div class="row">

                <div class="col-2">Кого показывать?</div>
                <div class="col-3">
                    <select class="form-select col-3" name="show">
                        <option value="plants" {{ psel }}>Растения</option>
                        <option value="zombies" {{ zsel }}>Зомби</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary col-3">Показать</button>

            </div>
        </form>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Название</th>
                    <th scope="col">Описание</th>
                </tr>
            </thead>
            
            <tbody class="plants {{ plantsDNone }}">
                <a href="/admin/new/plants" class="btn btn-primary {{ plantsDNone }}">Создать новое растение</a>
                {{#each plants}}
                <tr>
                    <th scope="row">{{ this.id }}</th>
                    <td>{{ this.name }}</td>
                    <td>{{ this.description }}</td>
                    <td><a href="/admin/change/plants?id={{ this.id }}" class="btn btn-primary">Редактировать</a></td>
                </tr>
                {{/each}}
            </tbody>
            <tbody class="zombies {{ zombiesDNone }}">
                <a href="/admin/new/zombies" class="btn btn-primary {{ zombiesDNone }}">Создать нового зомби</a>
                {{#each zombies}}
                <tr>
                    <th scope="row">{{ this.id }}</th>
                    <td>{{ this.name }}</td>
                    <td>{{ this.description }}</td>
                    <td><a href="/admin/change/zombies?id={{ this.id }}" class="btn btn-secondary">Редактировать</a></td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>